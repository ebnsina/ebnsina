<script lang="ts">
	// Bangla notes index — reuses the shared index view in the `bn` locale.
	import NotesIndexView from '$lib/components/notes/NotesIndexView.svelte';
</script>

<NotesIndexView locale="bn" />
